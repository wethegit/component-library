import { Meta } from "@storybook/blocks"

<Meta title="styles/global" />

## Global styles

**Location:** `<alias-to>/styles/global.scss`

This file contains the global styles and it also imports the reset stylesheet by default.