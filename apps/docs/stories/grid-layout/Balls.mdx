import { Canvas, Meta } from '@storybook/blocks'

import * as RowStories from './row.stories'

# Row

A Row is a square box that can be activated or deactivated when ticked.

Use Rowes to select one or more options from a list of choices.

<Meta of={RowStories} />
